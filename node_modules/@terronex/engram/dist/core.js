"use strict";
/**
 * Engram Core Implementation
 *
 * Reader, Writer, and Tree operations for .engram files
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.MemoryTree = void 0;
exports.generateId = generateId;
exports.getDecayTier = getDecayTier;
exports.touchNode = touchNode;
exports.isExpired = isExpired;
exports.cosineSimilarity = cosineSimilarity;
exports.searchNodes = searchNodes;
exports.searchNodesHNSW = searchNodesHNSW;
exports.searchNodesBruteForce = searchNodesBruteForce;
exports.haversineDistance = haversineDistance;
exports.euclideanDistance = euclideanDistance;
exports.spatialRecall = spatialRecall;
exports.findNearby = findNearby;
exports.createNode = createNode;
exports.createLink = createLink;
const types_1 = require("./types");
const hnswlib_node_1 = require("hnswlib-node");
// ============== CONSTANTS ==============
const MS_PER_DAY = 24 * 60 * 60 * 1000;
// ============== ID GENERATION ==============
let idCounter = 0;
function generateId() {
    const timestamp = Date.now().toString(36);
    const random = Math.random().toString(36).substring(2, 8);
    const counter = (idCounter++).toString(36).padStart(4, '0');
    return `${timestamp}-${random}-${counter}`;
}
// ============== TREE OPERATIONS ==============
class MemoryTree {
    constructor(nodes = [], hnswConfig) {
        this.nodes = new Map();
        this.rootIds = new Set();
        this.hnswIndex = null;
        this.idToLabel = new Map(); // Maps node ID to HNSW label
        this.labelToId = new Map(); // Maps HNSW label to node ID
        this.nextLabel = 0;
        this.hnswConfig = null;
        // Initialize HNSW if config provided
        if (hnswConfig) {
            this.initializeHNSW(hnswConfig);
        }
        for (const node of nodes) {
            this.nodes.set(node.id, node);
            if (!node.parentId) {
                this.rootIds.add(node.id);
            }
            // Add to HNSW index if embedding exists
            if (this.hnswIndex && node.embedding) {
                this.addToHNSW(node.id, node.embedding);
            }
        }
    }
    initializeHNSW(config) {
        this.hnswConfig = config;
        this.hnswIndex = new hnswlib_node_1.HierarchicalNSW(config.space, config.numDimensions);
        if (config.maxElements) {
            this.hnswIndex.initIndex(config.maxElements, config.M, config.efConstruction, config.randomSeed, config.allowReplaceDeleted);
        }
    }
    addToHNSW(nodeId, embedding) {
        if (!this.hnswIndex)
            return;
        const label = this.nextLabel++;
        this.idToLabel.set(nodeId, label);
        this.labelToId.set(label, nodeId);
        // Convert Float32Array to regular Array for hnswlib-node
        const embeddingArray = Array.from(embedding);
        this.hnswIndex.addPoint(embeddingArray, label);
    }
    removeFromHNSW(nodeId) {
        if (!this.hnswIndex)
            return;
        const label = this.idToLabel.get(nodeId);
        if (label !== undefined) {
            this.hnswIndex.markDelete(label);
            this.idToLabel.delete(nodeId);
            this.labelToId.delete(label);
        }
    }
    // Basic operations
    get(id) {
        return this.nodes.get(id);
    }
    getAll() {
        return Array.from(this.nodes.values());
    }
    getRoots() {
        return Array.from(this.rootIds).map(id => this.nodes.get(id));
    }
    size() {
        return this.nodes.size;
    }
    // Tree navigation
    getParent(id) {
        const node = this.nodes.get(id);
        if (!node?.parentId)
            return null;
        return this.nodes.get(node.parentId) || null;
    }
    getChildren(id) {
        const node = this.nodes.get(id);
        if (!node)
            return [];
        return node.children.map(cid => this.nodes.get(cid)).filter(Boolean);
    }
    getSiblings(id) {
        const node = this.nodes.get(id);
        if (!node)
            return [];
        const parentId = node.parentId;
        if (!parentId) {
            // Root siblings
            return Array.from(this.rootIds)
                .filter(rid => rid !== id)
                .map(rid => this.nodes.get(rid));
        }
        const parent = this.nodes.get(parentId);
        if (!parent)
            return [];
        return parent.children
            .filter(cid => cid !== id)
            .map(cid => this.nodes.get(cid))
            .filter(Boolean);
    }
    getAncestors(id) {
        const ancestors = [];
        let current = this.nodes.get(id);
        while (current?.parentId) {
            const parent = this.nodes.get(current.parentId);
            if (!parent)
                break;
            ancestors.push(parent);
            current = parent;
        }
        return ancestors;
    }
    getDescendants(id) {
        const descendants = [];
        const queue = [id];
        while (queue.length > 0) {
            const currentId = queue.shift();
            const node = this.nodes.get(currentId);
            if (!node)
                continue;
            for (const childId of node.children) {
                const child = this.nodes.get(childId);
                if (child) {
                    descendants.push(child);
                    queue.push(childId);
                }
            }
        }
        return descendants;
    }
    // Tree modification
    add(node) {
        this.nodes.set(node.id, node);
        if (!node.parentId) {
            this.rootIds.add(node.id);
        }
        else {
            const parent = this.nodes.get(node.parentId);
            if (parent && !parent.children.includes(node.id)) {
                parent.children.push(node.id);
            }
        }
        // Add to HNSW index if embedding exists
        if (node.embedding) {
            this.addToHNSW(node.id, node.embedding);
        }
        return node.id;
    }
    addChild(parentId, partialNode) {
        const parent = this.nodes.get(parentId);
        if (!parent)
            throw new Error(`Parent ${parentId} not found`);
        // Leverage createNode to initialize with defaults, then override with parent-specific info
        const childNode = createNode(partialNode.content.data, {
            type: partialNode.content.type,
            tags: partialNode.metadata?.tags,
            metadata: partialNode.metadata?.custom,
            // parentId and other tree-specific properties will be set next
        });
        // Manually set tree-specific properties derived from the parent
        childNode.parentId = parentId;
        childNode.depth = parent.depth + 1;
        childNode.path = `${parent.path}/${childNode.id}`;
        childNode.children = []; // Ensure it's an empty array initially for a new child
        // Merge any other properties from partialNode
        Object.assign(childNode, partialNode);
        return this.add(childNode);
    }
    update(id, updates) {
        const node = this.nodes.get(id);
        if (!node)
            throw new Error(`Node ${id} not found`);
        // Update modified time
        const updated = {
            ...node,
            ...updates,
            temporal: {
                ...node.temporal,
                ...updates.temporal,
                modified: Date.now()
            }
        };
        this.nodes.set(id, updated);
    }
    delete(id, cascade = false) {
        const node = this.nodes.get(id);
        if (!node)
            return;
        // Remove from HNSW index first
        this.removeFromHNSW(id);
        if (cascade) {
            // Delete all descendants
            for (const descendant of this.getDescendants(id)) {
                this.removeFromHNSW(descendant.id);
                this.nodes.delete(descendant.id);
            }
        }
        else {
            // Reparent children to grandparent
            for (const childId of node.children) {
                const child = this.nodes.get(childId);
                if (child) {
                    child.parentId = node.parentId;
                    if (node.parentId) {
                        const grandparent = this.nodes.get(node.parentId);
                        if (grandparent) {
                            grandparent.children.push(childId);
                        }
                    }
                    else {
                        this.rootIds.add(childId);
                    }
                }
            }
        }
        // Remove from parent's children
        if (node.parentId) {
            const parent = this.nodes.get(node.parentId);
            if (parent) {
                parent.children = parent.children.filter(cid => cid !== id);
            }
        }
        else {
            this.rootIds.delete(id);
        }
        this.nodes.delete(id);
    }
    move(id, newParentId) {
        const node = this.nodes.get(id);
        if (!node)
            throw new Error(`Node ${id} not found`);
        // Remove from old parent
        if (node.parentId) {
            const oldParent = this.nodes.get(node.parentId);
            if (oldParent) {
                oldParent.children = oldParent.children.filter(cid => cid !== id);
            }
        }
        else {
            this.rootIds.delete(id);
        }
        // Add to new parent
        if (newParentId) {
            const newParent = this.nodes.get(newParentId);
            if (!newParent)
                throw new Error(`New parent ${newParentId} not found`);
            newParent.children.push(id);
            node.parentId = newParentId;
            node.depth = newParent.depth + 1;
            node.path = `${newParent.path}/${id}`;
        }
        else {
            node.parentId = null;
            node.depth = 0;
            node.path = `/${id}`;
            this.rootIds.add(id);
        }
        // Update descendants' depths and paths
        this.updateDescendantPaths(id);
    }
    updateDescendantPaths(id) {
        const node = this.nodes.get(id);
        if (!node)
            return;
        for (const childId of node.children) {
            const child = this.nodes.get(childId);
            if (child) {
                child.depth = node.depth + 1;
                child.path = `${node.path}/${childId}`;
                this.updateDescendantPaths(childId);
            }
        }
    }
    // Search
    findByPath(path) {
        for (const node of this.nodes.values()) {
            if (node.path === path)
                return node;
        }
        return undefined;
    }
    findByDepth(depth) {
        return Array.from(this.nodes.values()).filter(n => n.depth === depth);
    }
    findByTag(tag) {
        return Array.from(this.nodes.values()).filter(n => n.metadata.tags?.includes(tag));
    }
    // HNSW utility methods
    hasHNSWIndex() {
        return this.hnswIndex !== null;
    }
    searchHNSW(queryEmbedding, k) {
        if (!this.hnswIndex) {
            throw new Error('HNSW index not initialized');
        }
        // Convert Float32Array to regular Array for hnswlib-node
        const queryArray = Array.from(queryEmbedding);
        const searchResults = this.hnswIndex.searchKnn(queryArray, k);
        return searchResults.neighbors.map((label, index) => ({
            nodeId: this.labelToId.get(label) || '',
            distance: searchResults.distances[index]
        })).filter(result => result.nodeId !== '');
    }
    buildHNSWIndex(config) {
        // Initialize HNSW if not already done
        if (!this.hnswIndex) {
            this.initializeHNSW(config);
        }
        // Add all existing nodes with embeddings
        for (const node of this.nodes.values()) {
            if (node.embedding && !this.idToLabel.has(node.id)) {
                this.addToHNSW(node.id, node.embedding);
            }
        }
    }
    getHNSWStats() {
        if (!this.hnswIndex)
            return null;
        return {
            totalElements: this.hnswIndex.getMaxElements(),
            currentCount: this.hnswIndex.getCurrentCount()
        };
    }
}
exports.MemoryTree = MemoryTree;
// ============== TEMPORAL OPERATIONS ==============
function getDecayTier(node, config = types_1.DEFAULT_DECAY_CONFIG) {
    const daysSinceAccess = (Date.now() - node.temporal.accessed) / MS_PER_DAY;
    if (daysSinceAccess < config.hotThresholdDays)
        return 'hot';
    if (daysSinceAccess < config.warmThresholdDays)
        return 'warm';
    if (daysSinceAccess < config.coldThresholdDays)
        return 'cold';
    return 'archive';
}
function touchNode(node) {
    return {
        ...node,
        temporal: {
            ...node.temporal,
            accessed: Date.now()
        }
    };
}
function isExpired(node) {
    if (!node.temporal.expires)
        return false;
    return Date.now() > node.temporal.expires;
}
// ============== SEARCH ==============
function cosineSimilarity(a, b) {
    if (a.length !== b.length)
        throw new Error('Vectors must have same length');
    let dot = 0;
    for (let i = 0; i < a.length; i++) {
        dot += a[i] * b[i];
    }
    return dot; // Assumes normalized vectors
}
function searchNodes(tree, queryEmbedding, options) {
    // Use HNSW search if available, otherwise fall back to brute force
    if (tree.hasHNSWIndex()) {
        return searchNodesHNSW(tree, queryEmbedding, options);
    }
    else {
        return searchNodesBruteForce(tree, queryEmbedding, options);
    }
}
function searchNodesHNSW(tree, queryEmbedding, options) {
    const { topK = 10, minScore = 0.5, filters, timeDecay = 0, includeArchived = false } = options;
    // Get initial candidates from HNSW (get more than topK to account for filtering)
    const searchK = Math.max(topK * 3, 100);
    const candidates = tree.searchHNSW(queryEmbedding, searchK);
    const results = [];
    for (const candidate of candidates) {
        const node = tree.get(candidate.nodeId);
        if (!node || !node.embedding)
            continue;
        // Apply filters
        if (filters) {
            if (filters.types && !filters.types.includes(node.content.type))
                continue;
            if (filters.tags && !filters.tags.some(t => node.metadata.tags?.includes(t)))
                continue;
            if (filters.decayTiers && !filters.decayTiers.includes(node.temporal.decayTier))
                continue;
            if (filters.paths && !filters.paths.some(p => node.path.startsWith(p)))
                continue;
            if (filters.dateRange) {
                const [start, end] = filters.dateRange;
                if (node.temporal.created < start || node.temporal.created > end)
                    continue;
            }
        }
        // Skip archived unless requested
        if (!includeArchived && node.temporal.decayTier === 'archive')
            continue;
        // Use distance from HNSW, convert to similarity
        let score = 1.0 - candidate.distance; // Assuming cosine distance, convert to similarity
        // Apply time decay
        if (timeDecay > 0) {
            const daysSinceAccess = (Date.now() - node.temporal.accessed) / MS_PER_DAY;
            const decayFactor = Math.exp(-timeDecay * daysSinceAccess);
            score *= decayFactor;
        }
        // Boost by quality score
        score *= (0.5 + 0.5 * node.quality.score);
        if (score >= minScore) {
            results.push({ node, score });
        }
    }
    // Sort by score and return top K
    results.sort((a, b) => b.score - a.score);
    return results.slice(0, topK);
}
function searchNodesBruteForce(tree, queryEmbedding, options) {
    const { topK = 10, minScore = 0.5, filters, timeDecay = 0, includeArchived = false } = options;
    const results = [];
    for (const node of tree.getAll()) {
        // Skip if no embedding
        if (!node.embedding)
            continue;
        // Apply filters
        if (filters) {
            if (filters.types && !filters.types.includes(node.content.type))
                continue;
            if (filters.tags && !filters.tags.some(t => node.metadata.tags?.includes(t)))
                continue;
            if (filters.decayTiers && !filters.decayTiers.includes(node.temporal.decayTier))
                continue;
            if (filters.paths && !filters.paths.some(p => node.path.startsWith(p)))
                continue;
            if (filters.dateRange) {
                const [start, end] = filters.dateRange;
                if (node.temporal.created < start || node.temporal.created > end)
                    continue;
            }
        }
        // Skip archived unless requested
        if (!includeArchived && node.temporal.decayTier === 'archive')
            continue;
        // Compute similarity
        let score = cosineSimilarity(queryEmbedding, node.embedding);
        // Apply time decay
        if (timeDecay > 0) {
            const daysSinceAccess = (Date.now() - node.temporal.accessed) / MS_PER_DAY;
            const decayFactor = Math.exp(-timeDecay * daysSinceAccess);
            score *= decayFactor;
        }
        // Boost by quality score
        score *= (0.5 + 0.5 * node.quality.score);
        if (score >= minScore) {
            results.push({ node, score });
        }
    }
    // Sort by score and return top K
    results.sort((a, b) => b.score - a.score);
    return results.slice(0, topK);
}
/**
 * Haversine distance between two lat/lon points
 * Returns distance in kilometers
 */
function haversineDistance(lat1, lon1, lat2, lon2) {
    const R = 6371; // Earth radius in km
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLon = (lon2 - lon1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
        Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
            Math.sin(dLon / 2) * Math.sin(dLon / 2);
    return R * 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
}
/**
 * Euclidean distance between two 2D/3D points
 */
function euclideanDistance(x1, y1, z1 = 0, x2, y2, z2 = 0) {
    const dx = x2 - x1;
    const dy = y2 - y1;
    const dz = z2 - z1;
    return Math.sqrt(dx * dx + dy * dy + dz * dz);
}
/**
 * Spatial recall: find memories within a radius of a point
 * Optionally filtered by semantic similarity
 */
function spatialRecall(tree, options) {
    const { queryEmbedding, center, radius, metric = 'euclidean', limit = 10, minScore = 0, includeArchived = false } = options;
    const results = [];
    // If semantic query provided, start with semantic search
    let candidates;
    let scoreMap = null;
    if (queryEmbedding) {
        // Get more candidates than needed to account for spatial filtering
        const semanticResults = searchNodes(tree, queryEmbedding, {
            query: '',
            topK: Math.max(limit * 5, 100),
            minScore: minScore,
            includeArchived
        });
        candidates = semanticResults.map(r => r.node);
        scoreMap = new Map(semanticResults.map(r => [r.node.id, r.score]));
    }
    else {
        // No semantic filter - consider all nodes
        candidates = tree.getAll();
    }
    // Filter by spatial distance
    for (const node of candidates) {
        // Skip nodes without position
        if (!node.position)
            continue;
        // Skip archived unless requested
        if (!includeArchived && node.temporal.decayTier === 'archive')
            continue;
        // Calculate distance
        let distance;
        if (metric === 'haversine') {
            // Treat x as latitude, y as longitude
            distance = haversineDistance(center.x, center.y, node.position.x, node.position.y);
        }
        else {
            distance = euclideanDistance(center.x, center.y, center.z ?? 0, node.position.x, node.position.y, node.position.z ?? 0);
        }
        // Check if within radius
        if (distance <= radius) {
            results.push({
                node,
                distance,
                score: scoreMap?.get(node.id)
            });
        }
    }
    // Sort by distance (closest first)
    results.sort((a, b) => a.distance - b.distance);
    return results.slice(0, limit);
}
/**
 * Find nodes near a specific node
 */
function findNearby(tree, nodeId, radius, options = {}) {
    const node = tree.get(nodeId);
    if (!node?.position) {
        throw new Error(`Node ${nodeId} not found or has no position`);
    }
    return spatialRecall(tree, {
        center: node.position,
        radius,
        metric: options.metric,
        limit: options.limit,
        includeArchived: options.includeArchived
    }).filter(r => r.node.id !== nodeId); // Exclude the source node
}
// ============== FACTORY ==============
function createNode(content, options = {}) {
    const id = generateId();
    const now = Date.now();
    return {
        id,
        parentId: options.parentId ?? null,
        children: [],
        depth: 0,
        path: `/${id}`,
        content: {
            type: options.type ?? 'text',
            data: content,
            tokens: Math.ceil(content.length / 4) // rough estimate
        },
        temporal: {
            created: now,
            modified: now,
            accessed: now,
            decayTier: 'hot'
        },
        quality: {
            score: 0.5,
            confidence: 1.0,
            source: 'direct'
        },
        metadata: {
            tags: options.tags,
            custom: options.metadata
        }
    };
}
function createLink(sourceId, targetId, type, options = {}) {
    return {
        id: generateId(),
        sourceId,
        targetId,
        type,
        confidence: options.confidence ?? 0.8,
        bidirectional: options.bidirectional ?? true,
        created: Date.now(),
        createdBy: options.createdBy ?? 'system'
    };
}
//# sourceMappingURL=core.js.map